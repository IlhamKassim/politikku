# Notes

*   User requested to learn about the current model used for the election model and why it is being used.
*   The repository enforces strict domain vocabulary. I must ensure the user understands the distinction between "Prediction" (avoid) and "Projection" (correct), as well as "Baseline", "Swing Model", and "Seat Call".
